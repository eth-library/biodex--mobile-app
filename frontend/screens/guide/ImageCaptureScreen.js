import React from 'react';
import { View, Text } from 'react-native';

function ImageCaptureScreen({ style }) {
  return (
    <View style={{ ...style }}>
      <Text>ImageCaptureScreen Screen</Text>
    </View>
  );
};

export default ImageCaptureScreen;
